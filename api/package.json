{
  "name": "tactiq-api",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && func start",
    "dev": "func start --typescript",
    "test:risk": "npm run build && node --test dist/shared/riskModel.test.js"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "openai": "^4.104.0",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
